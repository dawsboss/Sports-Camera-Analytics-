.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
# Match footage and every stage artifact stay on the homelab, never in git.
data/
spike/out/
*.mp4
*.mkv
*.mov
*.parquet
.env
# Model weights are downloaded, never committed; training runs and eval
# overlays are regenerated from them.
runs/
out/
*.pt
*.pth
*.onnx
