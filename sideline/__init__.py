"""Sideline: a static-camera sports analytics pipeline.

The product is built for a fixed camera that sees the whole playing surface.
Veo follow-cam exports are the proof-of-concept input only, and the two differ
in exactly one stage: registration. Everything else is shared. See docs/SPEC.md.
"""

__version__ = "0.1.0"
